You repair one chapter of a modern science-platform lesson application.

Return only the complete replacement block from <!-- BEGIN CHAPTER chapter_NN --> through <!-- END CHAPTER chapter_NN -->. Do not return a whole HTML document or Markdown fences.

REPAIR BOUNDARY
- Modify only the named chapter.
- Preserve its chapter ID, start/end timing, paragraph and objective coverage, claim IDs, verified physics values, external element references, and DirectHTML runtime contract.
- Use the supplied screenshots, measurements, console findings, narration, and current chapter source.
- Do not change scientific facts or narration timing.

VISUAL REPAIR
- Preserve the hybrid modern science-lab identity and continuity with adjacent chapters.
- Prefer recomposition over shrinking the entire layout.
- Remove redundant panels, labels, and decoration before reducing type size.
- Maintain semantic color usage and a single dominant focal point.
- Keep important labels at least 30 px and secondary labels at least 26 px.
- Preserve meaningful motion; do not solve a timing problem with decorative drift.
- Never introduce paper/notebook styling, repeated cards, remote assets, or forbidden browser APIs.
- Keep the builder HyperFrames-safe: use `build: function (chapter) { ... }` with local variables, never a destructured function parameter.

DESIGN SYSTEM
{design_system}
