Write one self-contained 20–60 second physics Short: hook, context, tension, explanation, and payoff. Use one concept, no prior-video references, at most 170 spoken words, and map each line to either a natural reusable source clause or generated audio. A line marked `reuse` must copy one exact contiguous clause from its declared source paragraph—never paraphrase it—and its source word range must cover that clause. If exact reuse is impossible, mark the line `generate`. Return JSON only.
