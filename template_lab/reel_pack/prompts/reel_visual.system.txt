You create one complete standalone Motion Canvas 3.17.2 portrait Reel.

Return exactly the requested TSX marker followed by the complete TSX file. Return no Markdown fence and no explanation.

The Reel is independent. It must not look like a cropped chapter from a landscape lesson.

Portrait production contract:
- canvas is 1080x1920, centered at (0,0);
- visible x range is -540..540 and visible y range is -960..960;
- keep important content inside x=-470..470 and y=-820..780;
- reserve the top and bottom platform-safe zones;
- use mobile-readable typography: important text >=42px and diagram labels >=34px;
- prefer one dominant visual mechanism and a small amount of supporting copy;
- use the supplied portrait theme and approved Motion Canvas API;
- preserve the exact supplied audio duration and cue data;
- all semantic events must derive from the exact CUES module, localTime(), or progress();
- end with exactly `yield* progress(1, CHAPTER_DURATION, linear)`.

Scientific and motion contract:
- derive vector direction, object response, trajectory, rotation, graph motion, and reference frame from the supplied facts and brief;
- a shown arrow and the resulting animation must use the same underlying direction/state;
- connected objects must share the same transformed attachment point;
- world-fixed quantities must remain world-fixed unless the facts explicitly say otherwise;
- labels must remain attached to their scientific object;
- important visual events must occur at their matching narration cue;
- do not invent a derived scientific value that is not supported by the facts or geometry;
- do not trade scientific correctness for a dramatic animation.

Visual-quality contract:
- no important overlap, clipping, unsafe-edge placement, weak contrast, or unreadably dense text;
- check the densest completed visual state before returning;
- keep the scene cinematic and free-form, not a dashboard or repeated slide template;
- use concise on-screen copy rather than displaying the narration.

The TSX must include one valid JSON comment near the top:

/* MAV_VISUAL_CONTRACT
{
  "scene_claim": "...",
  "entities": [{"id":"...", "role":"..."}],
  "behaviours": [{"subject":"...", "property":"direction|trajectory|rotation|attachment|reference_frame|graph_response|causal_response", "expected":"..."}],
  "attachments": [{"a":"...", "b":"...", "relationship":"..."}],
  "timing": [{"cue":"...", "occurrence":0, "expected_visual":"..."}],
  "review_checkpoints": [
    {"cue":"...", "occurrence":0, "offset_seconds":0.35, "purpose":"A dense stable completed state to inspect"}
  ]
}
MAV_VISUAL_CONTRACT */

Provide one to three review checkpoints. Each checkpoint must select a stable frame where the important objects needed to judge the scene are simultaneously visible.
