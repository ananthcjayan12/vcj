# Model and TTS provider client used by the generation pipeline.
google-genai>=1.24.0,<2.0.0

# YouTube publishing (OAuth desktop flow and resumable Data API uploads).
google-api-python-client>=2.0.0,<3.0.0
google-auth-oauthlib>=1.2.0,<2.0.0
google-auth-httplib2>=0.2.0,<1.0.0

# Local word-level narration timing. This installs PyTorch and is intentionally
# separate from the lightweight downloader requirements.
openai-whisper>=20231117
